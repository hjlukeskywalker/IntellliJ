server.port: 8081
spring.mvc:
  view:
    prefix: /WEB-INF/views/
    suffix: .jsp

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/test
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
